<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:backgroundTint="#C9E6F0"
    android:layout_marginBottom="8dp"
    android:padding="16dp"
    android:background="#C9E6F0">

    <!-- Section numéro et solde -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"

        android:gravity="center_vertical">

        <TextView
            android:id="@+id/textNumero"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="#000B58"
            android:textStyle="bold"
            android:textSize="16sp"
            android:text="Numéro" />

        <TextView
            android:id="@+id/textSolde"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#6A1E55"
            android:textStyle="bold"
            android:text="Solde" />
    </LinearLayout>

    <!-- Section type -->
    <TextView
        android:id="@+id/textType"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="#605EA1"
        android:textStyle="bold"
        android:text="Type" />

    <!-- Section date -->
    <TextView
        android:id="@+id/textDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"

        android:textColor="@android:color/black"
        android:text="Date : 01/01/2024"
        android:textStyle="italic" />

    <!-- Section des actions avec des icônes -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="16dp"
        android:gravity="center">

        <ImageButton
            android:id="@+id/buttonDelete"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Supprimer"
            android:src="@drawable/ic_delete"
            app:tint="@android:color/holo_red_dark" />

        <ImageButton
            android:id="@+id/buttonUpdate"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Modifier"
            android:src="@drawable/ic_edit"
            app:tint="@android:color/holo_blue_dark" />
    </LinearLayout>
</LinearLayout>
